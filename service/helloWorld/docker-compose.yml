version: '3.1'
services:
    spring:
        build:
            dockerfile: ./docker/Dockerfile-Spring
            context: .
        ports:
            - "9900:9901"
        container_name: "testserver"
        networks:
            - kolschnet
        restart: always

networks:
    kolschnet:
        external:
            name: kolschnet