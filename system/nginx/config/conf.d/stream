stream {
  # Configuration specific to TCP/UDP and affecting all virtual servers
  server {
    # configuration of TCP virtual server 1

    listen 25565;
    server_name  minecraft.kolschnet.de;
    proxy_pass minecraft:25565;
  }
}