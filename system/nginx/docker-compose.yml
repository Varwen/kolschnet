version: '3.1'
services:
    reverseproxy:
        image: nginx:1.21
        container_name: "reverseproxy"
        ports:
            - "80:80"
        networks:
            - kolschnet
        restart: always
        volumes:
            - "./config:/etc/nginx/conf.d"
networks:
    kolschnet:
        external:
            name: kolschnet